package timeline

// ErrAlreadyInitialized and other sentinel errors are package-level vars,
// so they are already accessible to the _test package via the exported name.
// This file intentionally left minimal — all helpers live in engine_test.go.
